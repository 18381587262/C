#include<stdio.h>
#define n 10

int main()
{
	int a[n], k, i, j;
	for (i = 0; i < n; i++)
	{
		scanf("%d", &a[i]);
	}
	scanf("%d", &k);
	k += 30; //直接踩凳子
	for (i = 0,j = 0; i < n; i++)
	{
		if (a[i] <= k) j++;
	}
	printf("%d", j);
	return 0;
}						
